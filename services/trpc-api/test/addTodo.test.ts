import { beforeEach, describe, expect, it, vi } from 'vitest'

describe('addTodo', () => {
	beforeEach(() => {
		vi.resetAllMocks()
	})

	it('should succeed', async () => {
		expect(1).toEqual(1)
	})
})
