import { Secrets } from './types'

const mockedSecrets: Secrets = {
	/*
		BOILERPLATE CODE
		replace with your secrets
	*/
	SECRET_EXAMPLE: {
		somethingSuperSecret: 'mocked'
	}
}

export default mockedSecrets
